import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function budgets() {
	return (
		<View className="flex h-full border-2 justify-center items-center">
			<Text className="text-7xl">budgets</Text>
			<Text> This is the budgets screen </Text>
		</View>
	);
}

const styles = StyleSheet.create({});
